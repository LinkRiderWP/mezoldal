# Szerver beállítások
PORT=3000
BASE_URL=http://localhost:3000

# Hitelesítés (JWT kulcs) - Generálás: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=ide_kell_a_biztonsagos_veletlenszeru_hosszu_kulcs

# Firebase Admin (opcionális, ha nem fájlból, hanem env-ből töltöd be JSON stringként)
# FIREBASE_SERVICE_ACCOUNT_JSON=

# OTP SimplePay beállítások
SIMPLEPAY_SANDBOX=true
SIMPLEPAY_MERCHANT=
SIMPLEPAY_SECRET_KEY=

# Billingo beállítások
BILLINGO_API_KEY=
BILLINGO_BLOCK_ID=

# E-mail küldés (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info@miklomez.hu
SMTP_PASS=jelszo_vagy_app_jelszo
EMAIL_FROM="Mikló Méhészet" <info@miklomez.hu>